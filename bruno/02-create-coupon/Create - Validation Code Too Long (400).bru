meta {
  name: Create - Validation Code Too Long (400)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/coupons
  body: json
  auth: none
}

body:json {
  {
    "code": "THISCODEISWAYTOOLONG",
    "maxUsage": 10,
    "countryCode": "PL"
  }
}

tests {
  test("returns 400 Bad Request", function () {
    expect(res.getStatus()).to.equal(400);
  });
}