meta {
  name: Create - Success (SUMMER2026)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/coupons
  body: json
  auth: none
}

body:json {
  {
    "code": "SUMMER2026",
    "maxUsage": 100,
    "countryCode": "PL"
  }
}

tests {
  test("returns 201 Created", function () {
    expect(res.getStatus()).to.equal(201);
  });
  test("body has no internal id field", function () {
    expect(res.getBody().id).to.be.undefined;
  });
}