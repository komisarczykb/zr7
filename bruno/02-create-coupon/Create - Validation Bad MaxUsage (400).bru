meta {
  name: Create - Validation Bad MaxUsage (400)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/coupons
  body: json
  auth: none
}

body:json {
  {
    "code": "BADQTY",
    "maxUsage": 0,
    "countryCode": "PL"
  }
}

tests {
  test("returns 400 Bad Request", function () {
    expect(res.getStatus()).to.equal(400);
  });
}